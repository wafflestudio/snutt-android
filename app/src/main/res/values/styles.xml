<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- RemoveLecture shadow below action bar Android < 5.0 -->
        <item name="android:windowContentOverlay">@null</item>

        <!--For EditText-->
        <item name="editTextStyle">@style/EditTextStyle</item>

        <!--For Background color-->
        <item name="android:windowBackground">@color/white</item>

        <!--For SearchView home indicator -->
        <item name="homeAsUpIndicator">@drawable/ic_back</item>
        <item name="android:homeAsUpIndicator">@drawable/ic_back</item>

        <!-- 최신 안드로이드 UI 트렌드는 앱 콘텐츠가 화면 전체를 사용하는 'Edge-to-Edge' 경험을 제공하는 것. -->
        <!-- target SDK 35 대응하면서 추가. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="EditTextStyle" parent="Widget.AppCompat.EditText">
        <item name="android:textColorHint">#34000000</item>
        <item name="android:textCursorDrawable">@drawable/text_cursor</item>
        <item name="android:background">@drawable/edittext_background</item>
    </style>

    <style name="AppBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/background_search_option</item>
    </style>

    <!-- Text -->

    <!-- For searched lecture list -->

    <!-- For my lecture list-->

    <!--For notification-->

    <!--For lecture detail-->

    <!--For timetable list-->


    <!--For Welcome page-->

    <!--For Table Create -->

    <!-- For Notification Placeholder -->

    <!--For TagHelper-->

    <!--For report Fragment-->

    <style name="popup_dialog" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowMinWidthMajor">100%</item>
        <item name="windowMinWidthMinor">100%</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="Theme.Snutt.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">?android:attr/colorBackground</item>
        <item name="postSplashScreenTheme">@style/AppTheme</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/snutt_splash_icon</item>
    </style>

</resources>
